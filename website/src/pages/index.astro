---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Smith - Multi-Agent AI Development">
  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="container">
        <div class="hero-content">
          <h1 class="hero-title">
            <span class="gradient-text">Smith</span>
          </h1>
          <p class="hero-subtitle">
            Multi-agent AI development system
          </p>
          <p class="hero-description">
            Orchestrate specialized AI agents to plan, implement, test, and review your code.
            One command. One chat. Multiple agents working together.
          </p>
          <div class="hero-actions">
            <a href="#install" class="btn btn-primary">
              Get Started
            </a>
            <a href="https://github.com/speier/smith" class="btn btn-secondary" target="_blank" rel="noopener">
              View on GitHub
            </a>
          </div>
        </div>
      </div>
      <div class="hero-gradient"></div>
    </section>

    <!-- Features Section -->
    <section class="features">
      <div class="container">
        <h2 class="section-title">Why Smith?</h2>
        <div class="features-grid">
          <div class="feature-card">
            <div class="feature-icon">🤖</div>
            <h3>Multi-Agent System</h3>
            <p>Specialized agents for planning, implementation, testing, and code review work together seamlessly.</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🎯</div>
            <h3>Safety First</h3>
            <p>Configurable auto-levels (off, low, medium, high) ensure AI actions match your comfort level.</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">⚡</div>
            <h3>Provider Choice</h3>
            <p>Works with GitHub Copilot, OpenRouter, and OpenAI. Use your preferred provider and models.</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🎨</div>
            <h3>Beautiful TUI</h3>
            <p>Modern terminal interface with streaming responses, real-time updates, and intuitive slash commands.</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">📦</div>
            <h3>Project-Centric</h3>
            <p>Each project has its own configuration. No global defaults forcing choices on you.</p>
          </div>
          <div class="feature-card">
            <div class="feature-icon">🔧</div>
            <h3>Developer Control</h3>
            <p>You're in charge. Review plans, approve changes, customize agent behavior per project.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Installation Section -->
    <section id="install" class="install">
      <div class="container">
        <h2 class="section-title">Get Started</h2>
        <div class="install-steps">
          <div class="step">
            <div class="step-number">1</div>
            <div class="step-content">
              <h3>Install Smith</h3>
              <pre><code>curl -fsSL https://agentsmith.sh/install | bash</code></pre>
              <p class="step-note">Or with Go: <code>go install github.com/speier/smith@latest</code></p>
            </div>
          </div>
          <div class="step">
            <div class="step-number">2</div>
            <div class="step-content">
              <h3>Start the REPL</h3>
              <pre><code>cd your-project
smith</code></pre>
              <p class="step-note">Smith launches directly into an interactive chat session.</p>
            </div>
          </div>
          <div class="step">
            <div class="step-number">3</div>
            <div class="step-content">
              <h3>Start Building</h3>
              <pre><code>💬 You: Add user authentication with JWT
🤖 Smith: I'll help with that...</code></pre>
              <p class="step-note">Chat naturally. Use slash commands like /status, /agents, /settings.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- How It Works Section -->
    <section class="how-it-works">
      <div class="container">
        <h2 class="section-title">How It Works</h2>
        <div class="workflow">
          <div class="workflow-step">
            <div class="workflow-badge">Planning Agent</div>
            <p>Analyzes your request and creates a detailed implementation plan with tasks.</p>
          </div>
          <div class="workflow-arrow">→</div>
          <div class="workflow-step">
            <div class="workflow-badge">Implementation Agent</div>
            <p>Executes tasks, writes code, and makes changes to your codebase.</p>
          </div>
          <div class="workflow-arrow">→</div>
          <div class="workflow-step">
            <div class="workflow-badge">Testing Agent</div>
            <p>Creates comprehensive test suites to validate the implementation.</p>
          </div>
          <div class="workflow-arrow">→</div>
          <div class="workflow-step">
            <div class="workflow-badge">Review Agent</div>
            <p>Reviews code quality, suggests improvements, ensures best practices.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta">
      <div class="container">
        <h2>Ready to accelerate your development?</h2>
        <p>Just run <code>smith</code> and start chatting. It's that simple.</p>
        <div class="cta-actions">
          <a href="#install" class="btn btn-primary btn-large">
            Get Started Now
          </a>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
      <div class="container">
        <p>Built with ❤️ for developers</p>
        <div class="footer-links">
          <a href="https://github.com/speier/smith" target="_blank" rel="noopener">GitHub</a>
          <a href="https://github.com/speier/smith/issues" target="_blank" rel="noopener">Issues</a>
          <a href="https://github.com/speier/smith/blob/main/README.md" target="_blank" rel="noopener">Docs</a>
        </div>
      </div>
    </footer>
  </main>
</Layout>

<style>
  /* Container */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Hero Section */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .hero-gradient {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 50% 50%, rgba(99, 102, 241, 0.15) 0%, transparent 50%);
    pointer-events: none;
    z-index: 0;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
  }

  .hero-title {
    font-size: clamp(3rem, 10vw, 6rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1rem;
    letter-spacing: -0.02em;
  }

  .gradient-text {
    background: linear-gradient(135deg, var(--color-primary-light) 0%, var(--color-accent) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    color: var(--color-text-secondary);
    margin-bottom: 1.5rem;
    font-weight: 500;
  }

  .hero-description {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: var(--color-text-secondary);
    margin-bottom: 3rem;
    line-height: 1.8;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Buttons */
  .btn {
    display: inline-block;
    padding: 0.875rem 2rem;
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 0.5rem;
    transition: all 0.2s ease;
    border: 2px solid transparent;
  }

  .btn-primary {
    background: var(--color-primary);
    color: white;
  }

  .btn-primary:hover {
    background: var(--color-primary-light);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(99, 102, 241, 0.3);
  }

  .btn-secondary {
    background: transparent;
    color: var(--color-text);
    border-color: var(--color-text-secondary);
  }

  .btn-secondary:hover {
    border-color: var(--color-text);
    background: rgba(255, 255, 255, 0.05);
  }

  .btn-large {
    padding: 1.125rem 2.5rem;
    font-size: 1.125rem;
  }

  /* Features Section */
  .features {
    padding: 8rem 0;
    background: var(--color-surface);
  }

  .section-title {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 700;
    text-align: center;
    margin-bottom: 4rem;
    background: linear-gradient(135deg, var(--color-text) 0%, var(--color-text-secondary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .feature-card {
    padding: 2rem;
    background: var(--color-bg);
    border-radius: 1rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    transition: all 0.3s ease;
  }

  .feature-card:hover {
    transform: translateY(-4px);
    border-color: var(--color-primary);
    box-shadow: 0 8px 32px rgba(99, 102, 241, 0.2);
  }

  .feature-icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    font-size: 1.5rem;
    margin-bottom: 0.75rem;
    color: var(--color-text);
  }

  .feature-card p {
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  /* Installation Section */
  .install {
    padding: 8rem 0;
  }

  .install-steps {
    max-width: 800px;
    margin: 0 auto;
  }

  .step {
    display: flex;
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .step-number {
    flex-shrink: 0;
    width: 3rem;
    height: 3rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-primary);
    color: white;
    border-radius: 50%;
    font-weight: 700;
    font-size: 1.25rem;
  }

  .step-content {
    flex: 1;
  }

  .step-content h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }

  .step-content pre {
    background: var(--color-surface);
    padding: 1rem 1.5rem;
    border-radius: 0.5rem;
    overflow-x: auto;
    margin: 1rem 0;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .step-content code {
    color: var(--color-accent);
    font-size: 0.95rem;
  }

  .step-note {
    color: var(--color-text-secondary);
    font-size: 0.95rem;
    margin-top: 0.5rem;
  }

  /* How It Works Section */
  .how-it-works {
    padding: 8rem 0;
    background: var(--color-surface);
  }

  .workflow {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    flex-wrap: wrap;
    max-width: 1000px;
    margin: 0 auto;
  }

  .workflow-step {
    flex: 1;
    min-width: 200px;
    text-align: center;
    padding: 1.5rem;
  }

  .workflow-badge {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
    color: white;
    border-radius: 2rem;
    font-weight: 600;
    margin-bottom: 1rem;
    font-size: 0.95rem;
  }

  .workflow-step p {
    color: var(--color-text-secondary);
    line-height: 1.6;
  }

  .workflow-arrow {
    font-size: 2rem;
    color: var(--color-primary);
    flex-shrink: 0;
  }

  @media (max-width: 768px) {
    .workflow-arrow {
      display: none;
    }
  }

  /* CTA Section */
  .cta {
    padding: 8rem 0;
    text-align: center;
  }

  .cta h2 {
    font-size: clamp(2rem, 5vw, 3rem);
    margin-bottom: 1rem;
  }

  .cta p {
    font-size: 1.25rem;
    color: var(--color-text-secondary);
    margin-bottom: 2rem;
  }

  .cta p code {
    background: var(--color-surface);
    padding: 0.25rem 0.75rem;
    border-radius: 0.375rem;
    font-size: 1.1rem;
    color: var(--color-accent);
  }

  .cta-actions {
    margin-top: 2rem;
  }

  /* Footer */
  .footer {
    padding: 3rem 0;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    text-align: center;
  }

  .footer p {
    color: var(--color-text-secondary);
    margin-bottom: 1rem;
  }

  .footer-links {
    display: flex;
    gap: 2rem;
    justify-content: center;
  }

  .footer-links a {
    color: var(--color-text-secondary);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .footer-links a:hover {
    color: var(--color-primary);
  }
</style>
